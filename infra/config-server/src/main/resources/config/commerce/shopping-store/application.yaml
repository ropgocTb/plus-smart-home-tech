server:
  port: 0

spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/postgres-shop-db
    username: username
    password: password

  jpa:
    hibernate:
      ddl-auto: update