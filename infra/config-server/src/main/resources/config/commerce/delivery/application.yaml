server:
  port: 0

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5438/postgres-delivery-db
    username: username
    password: password

  jpa:
    hibernate:
      ddl-auto: update